<div class="hero-unit">
  <h1>Nowa strona</h1>

  <%= form_for(:strona, :url => {:action =>'utworz', :kategoria_id => @kategorie.id}) do |pole| %>

  <%= render(:partial => "form", :locals => {:pole => pole}) %>

  <div style="text-align: center;">
  <%= link_to("Powrót", {:action=>'index', :kategoria_id => @kategorie.id}, :class=>'btn btn-warning') %>

    <%= submit_tag "Utwórz", class: "btn btn-mini btn-primary" %>
  </div>

  <% end %>

</div>
