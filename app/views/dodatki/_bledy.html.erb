
<% if @uzytkownik.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@uzytkownik.errors.count, "error") %> prohibited this article from being saved:</h2>

    <ul>
    <% objekt.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
